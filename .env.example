# Database Configuration
POSTGRES_DB=ratelimitx
POSTGRES_USER=ratelimitx
POSTGRES_PASSWORD=change_this_password_in_production

# Application Configuration
SPRING_DATASOURCE_URL=jdbc:postgresql://postgres:5432/ratelimitx
SPRING_DATASOURCE_USERNAME=ratelimitx
SPRING_DATASOURCE_PASSWORD=change_this_password_in_production

# JWT Secret - MUST be changed in production (use at least 256-bit key)
JWT_SECRET=change_this_jwt_secret_in_production_use_strong_random_key
